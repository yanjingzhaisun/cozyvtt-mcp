Opt-in marker added per the contributing guide's agent-PR process (`🤖🤖🤖` at the end of the title), with the disclosure the marker is meant to imply: this change was prepared by an AI agent (Hermes Agent, running locally) on behalf of the repository owner. Nothing here was auto-merged: the entry text, the badges, and the scope tags were each reviewed by the maintainer (yanjingzhaisun) before the push, which is why the commits carry human-authored commit messages rather than an unattended bot loop. Flagging that openly rather than letting the marker stand alone.

Also in this push: the scope tag is now 🏠 (Local Service) instead of ☁️ (Cloud Service). The bridge talks to a CozyVTT instance the operator installs and runs themselves — on their own machine or LAN — which is the README's stated rule for local ("MCP server is talking to a locally installed software"). The closest analogues in the Gaming section tag it the same way (`HadiCherkaoui/crafty-mcp` for Crafty Controller, `Tanshaydar/Quartermaster`). It remains an installable server (public repo, `pip`/`uv` or the bundled `Dockerfile`), so it belongs in this list rather than in awesome-remote-mcp-servers.

Glama evaluation is current, not stale: all 37 tools rate A on the tool-definition rubric — https://glama.ai/mcp/servers/yanjingzhaisun/cozyvtt-mcp/score (the badge image refreshes on Glama's own batch schedule, so the score page is the live view).
